server:
  port: 8080

spring:
  application:
    name: customer-service
  datasource:
    username: 'root'
    password: 'password'
    url: jdbc:mysql://localhost:3306/cigma_2023_tp2_springcloud_db?serverTimezone=UTC
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true
